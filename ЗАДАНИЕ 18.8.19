                                                                                  ЗАДАНИЕ
     Для онлайн-конференции необходимо написать программу, которая будет подсчитывать общую стоимость билетов. Программа должна работать следующим образом:

1. В начале у пользователя запрашивается количество билетов, которые он хочет приобрести на мероприятие.

2. Далее для каждого билета запрашивается возраст посетителя, в соответствии со значением которого выбирается стоимость:

Если посетителю конференции менее 18 лет, то он проходит на конференцию бесплатно.
От 18 до 25 лет — 990 руб.
От 25 лет — полная стоимость 1390 руб.
3. В результате программы выводится сумма к оплате. При этом, если человек регистрирует больше трёх человек на конференцию, то дополнительно получает 10% скидку на полную стоимость заказа.                                                                
 
                                                                                РЕШЕНИЕ

ВАРИАНТ 1:

ticket = int (input ('Введите колличество билетов: '))
age = list(map(int, input('Введите возраст посетителя(ей) (через пробел): ').split()))
number1 = 0 # Вводим переменные для параметров возраста
number2 = 0
for i in range(len(age)): # Определяем кол-во билетов по возрасту
       if 18<=age[i]<25:
         number1=number1+1
       elif age[i]>=25:
         number2=number2+1

if ticket>3: # Определяем стоимость с учетом скидки
    coust=(((number1*990)+(number2*1390))*90)/100
else: # Определяем стоимость с учетом скидки
    coust=(number1*990)+(number2*1390)

print ('Сумма к оплате: ', int(coust))

ВАРИАНТ 2:
# С повторным вводом после неправильного ввода данных

ticket = int (input ('Введите колличество билетов: '))
age = list(map(int, input('Введите возраст посетителя(ей): ').split()))
number1 = 0
number2 = 0
while True:
    if len(age)==ticket:
        for i in range(len(age)):
            if 18<=age[i]<25:
                number1=number1+1
            if age[i]>=25:
                number2=number2+1
        break
    else:
        print ("!!!Ошибка ввода!!!")
        age = list(map(int, input('Введите возраст посетителя(ей): ').split()))

if ticket>3:
    coust=(((number1*990)+(number2*1390))*90)/100
    print('Скидка 10%')
    print('Сумма к оплате: ', int(coust))
else:
    coust=(number1*990)+(number2*1390)
    print ('Сумма к оплате: ', int(coust))
